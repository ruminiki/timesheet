<?php
	// module/Point/view/point/point/add.phtml:
	$title = 'Novo Registro';
	$this->headTitle($title);

?>

<h3><?php echo $this->escapeHtml($title); ?></h3>

<?php
	$form = $this->form;
	$form->setAttribute('action', $this->url('point', array('action' => 'add')));
	$form->prepare();

	echo $this->form()->openTag($form);
	echo $this->formHidden($form->get('id'));
	echo $this->formRow($form->get('date'));
	echo $this->formRow($form->get('schedule'));
	echo $this->formRow($form->get('note'));
?>
	<input type="submit" name="option" value="Salvar" class="btn btn-primary"/>
	<input type="submit" name="option" value="Cancelar" class="btn btn-warning"/>
<?php
	echo $this->form()->closeTag();
?>